spring:

  config:
    activate:
      on-profile: dev

  datasource:
    url: jdbc:h2:mem:solostockhub
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  h2:
    console:
      enabled: true
      path: /h2-console

  sql:
    init:
      mode: never

logging:
  level:
    org.hibernate.SQL: DEBUG